package com.singh.grind75.week1;

public class InsertInterval {

    /*
        You are given an array of non-overlapping intervals) where intervals[i] = [standi, endi] represent
        the start and the end of the ith interval and intervals is sorted in ascending order by starti. You
        are also given an interval (newInterval = [start,end]) that represents the start and end of another interval

        Insert newInterval into intervals such that intervals is still sorted in ascending order by starti, and internals
        does not have any overlapping intervals (merge overlapping intervals if necessary)

        e.g.
        [[1,3],[6,9] ; [2,5]
        [[1,5],[6,9]]

        [[1,2],[3,5],[6,7],[8,10],[12,16]] ; [4,8]
        [[1,2],[3,10],[12,16]]
     */
    public int[][] insert(int[][] intervals, int[] newInterval) {
       return new int[][]{};
    }
}
