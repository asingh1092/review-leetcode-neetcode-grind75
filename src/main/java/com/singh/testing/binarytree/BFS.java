package com.singh.testing.binarytree;

public class BFS {

    // binary, or in general -> n-ary
    // full = all nodes have two children except for leaves
    // perfect = full binary tree where all the leaves are at the same depth and all parent nodes have two children
    // complete = every node is fulled except possibly the last  and to the left node
    // balanced = height left sub-tree and right sub-tree is equal or differs by most 1
    // BST = binary search tree = bianry search trees are used for various searching and sorting algos
    //                          -> everything on the left is less than the parent, and value of the right node is greater


}
